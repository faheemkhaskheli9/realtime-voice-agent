# Phase 1 runtime + test dependencies.
PyJWT>=2.8

# Optional: only needed to connect to a real LiveKit server (dry-run mode
# works without it). Install with: pip install -r requirements.txt livekit
# livekit>=0.11

# Testing
pytest>=7.4
